<% @users.each do |u|  %>
  <% current_user.followers.each do |follower| %>
    <p>
      <%= u.email %>
    </p>
    <%= form_for @request do |r| %>
    <%= r.hidden_field :invitor_id, value: current_user.id%>
    <%= r.hidden_field :invitee_id, value: u.id %>
    <%= r.hidden_field :accepted, value: false %>
    <!-- dont allow user to request themselves -->
    <% if current_user.email != u.email%>
      <%= r.submit "Request Friend"%>
    <% end %>
  <% end %>
<% end %>
<% end %>

<%# show request friend for users that current user is not following %>
